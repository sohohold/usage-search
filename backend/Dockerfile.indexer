FROM node:22-slim

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY scripts ./scripts
COPY tsconfig.json ./

# Allow passing args via CMD override:
#   docker compose run --rm indexer --limit 200
ENTRYPOINT ["npx", "tsx", "scripts/build-index.ts"]
